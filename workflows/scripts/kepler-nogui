#!/bin/bash

KEPLER_BUILDXML=${PWD}/kepler

if [ -z "$1" ]; then
  echo "Usage /usr/bin/kepler-nogui.sh $PATH/myWorkflow.xml"
  exit -1
fi

echo "Kepler download and Install"
. kepler_install
 
ant -buildfile ${KEPLER_BUILDXML}/build.xml run-workflow-no-gui -Dworkflow=$1
